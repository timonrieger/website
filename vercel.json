{
  "installCommand": "bash scripts/install-go.sh",
  "build": {
    "env": {
      "HUGO_VERSION": "0.155.2"
    }
  },
  "buildCommand": "export PATH=\"$HOME/.local/go/bin:$PATH\" && go version && hugo --gc --minify"
}
